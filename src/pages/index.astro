---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import SolarSystem from '../components/SolarSystem.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const projects = (await getCollection('projects')).sort(
	(a, b) => (a.data.order || 0) - (b.data.order || 0)
);
---

<!doctype html>
<html lang="ru">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} image="/images/solar.jpg" />
	</head>
	<body>
		<Header />
		<main>
			<h1>Ӟечбур! Привет! Hello! 👋</h1>
			<p>Я Григорий, инженер‑разработчик. В центре — я (Солнце), вокруг — мои проекты (планеты).</p>
			<SolarSystem projects={projects} name="Григорий" baseHref="" />
			<p style="margin-top: 1.5rem;"><a href="/projects">Посмотреть все проекты →</a></p>
		</main>
		<Footer />
	</body>
</html>

<style>
	main {
		width: calc(100% - 2em);
		max-width: 1100px;
	}
</style>
